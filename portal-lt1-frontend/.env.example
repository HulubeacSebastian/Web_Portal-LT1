# Vite incarca automat:
#   .env.development  -> npm run dev (local)
#   .env.production   -> npm run build (deploy)
#   .env.local        -> optional, gitignored, override pe PC

# Nu pune VITE_API_BASE_URL aici — foloseste fisierele de mai sus.
